[tool.black]
line-length = 100
target-version = ["py39"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
filterwarnings = [
    "ignore::sklearn.exceptions.UndefinedMetricWarning"
]
markers = [
    "integration: marca para pruebas de integración end-to-end"
]